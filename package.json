{
  "name": "pogo-paste",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf ./_site"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
